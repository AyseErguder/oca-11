package chapter02._07.identifiers;

public class Test02CamelCase {
    // Java has conventions so that code is readable and consistent

    /*
    Method and variable names are written in camelCase with the first letter being lowercase
     */

    /*
    Class and interface names are written in camelCase with the first
    letter being uppercase. Also, don’t start any class name with $, as
    the compiler uses this symbol for some files
     */

    /*
     Java supports the Unicode character set, so there are
    thousands of characters that can start a legal Java identifier
     */
    int ɰʃ = 10;

}
